# 视频截图神器

基于 React + Electron 的本地视频截图工具。

## 功能
- 导入本地视频，支持 MP4 / MKV / TS / AVI / MOV 等格式
- 逐帧快进快退，支持键盘和鼠标侧键长按连跳
- 竖图裁切，可手动拖动或启用 AI 跟踪
- 按区间、倍率或目标张数批量提取
- 截图自动保存到指定目录，并在侧栏预览
- 快捷键：`Space` 播放暂停，`S` / 右键截图，`I` / `O` 设置范围

## 构建

```bash
npm install
npm run dev
npm run build
```
