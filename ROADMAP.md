# 迭代计划

> 核心目标：**极致速度 → 极致清晰 → 极致不重复**

---

## ✅ v2.0.9（已完成）

**Browser Video API 极限优化**

- 采样封顶 96 次 seek，48s 视频 ~6s 完成
- dHash 感知哈希去重，按结构而非像素判重复
- 时间分段保证覆盖率 + Laplacian 清晰度评分
- Canvas 复用零 re-seek 输出

**瓶颈**：浏览器 video.seek 是串行操作，每次 ~100ms，无法突破

---

## 🔜 v2.1.0（下一版本）

**FFmpeg 引擎替换——突破 seek 瓶颈**

### 目标
- 48s 视频提取从 ~6s → **~1-2s**
- 支持更长视频（5-10min）恒定速度完成

### 实现方案
1. **FFmpeg 批量解码**：一次命令提取所有候选帧到临时目录，替代逐帧 seek
   - `ffmpeg -i input.mp4 -vf "fps=1,select='not(mod(n,30))'" -vsync vfn frame_%04d.jpg`
   - 直接解码视频流，速度是浏览器 seek 的 10-50 倍
2. **Node 子进程调用**：Electron 环境直接 `child_process.spawn('ffmpeg', ...)`
3. **分析管线不变**：提取后的图片仍通过 Canvas 做 Laplacian + dHash 分析
4. **FFmpeg 捆绑**：内置 ffmpeg.exe 或引导用户下载

### 风险点
- FFmpeg 二进制体积 (~80MB)，考虑按需下载
- 临时文件管理（提取完需清理）

---

## 🔮 v2.2.0（远期）

**AI 驱动的智能选帧**

### 目标
- 从"像素级去重"升级到"语义级去重"
- 自动识别关键姿态/动作节点

### 可能方向
1. **姿态估计模型**：MoveNet/BlazePose 检测骨骼关键点，按关节角度差异判重复
2. **场景变化检测**：自动识别镜头切换/场景转换点
3. **运动能量曲线**：计算光流能量，在能量低谷（静止点）优先取帧
4. **清晰度 + 美学评分**：引入 NIMA 等图像质量评估模型

---

## 优先级排序

| 优先级 | 方向 | 收益 | 复杂度 |
|--------|------|------|--------|
| P0 | FFmpeg 引擎（速度） | ⭐⭐⭐⭐⭐ | 中 |
| P1 | 清晰度评分优化 | ⭐⭐⭐ | 低 |
| P2 | 姿态估计去重 | ⭐⭐⭐⭐ | 高 |
| P3 | 美学评分 | ⭐⭐ | 高 |
