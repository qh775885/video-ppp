# 开发进度

> 最后更新：2026-03-14

---

## ✅ 已完成（v2.1.2）

### 1. 软件信息更新
- 更新关于界面的开源地址为作者哔站动态地址
- 简化关于页面信息：移除“开源免费版”字样，新增快捷键说明与参数限制提示

---

## ✅ 已完成（v2.1.1）

### 1. 动态提取策略
- 根据视频时长、帧率、目标张数预估两种方案耗时，自动选更快的
- Seek 模式：适合短视频低张数，每段独立 `-ss` 跳转
- 全段扫描：适合高张数，一次 FFmpeg 解码全部候选帧
- 扫描耗时公式校准：`时长 × (fps/30) × 0.025 + 候选总数 × 0.03`

### 2. 推荐张数幂律公式
- `d^0.6 × 3.5 × (30/fps)^0.3`
- 1min/30fps → ~42，5min/60fps → ~93，8min/30fps → ~144
- 高帧率视频自动降低推荐（冗余帧多 + 处理慢）
- 下限 10，无硬上限

### 3. 跨段 dHash 去重
- 每段选帧时与前一已选帧比对 Hamming 距离
- 太相似（<阈值）则换次优候选
- 阈值按帧率调整：30fps→20，60fps→25

### 4. Bug 修复
- 切换视频后 rangeStart/rangeEnd 未重置（长视频只提取前段）
- 切换视频后推荐张数未重算（duration 未归零导致 useEffect 不触发）
- DHASH_THRESHOLD 作用域错误（try 块内定义，函数外引用）

---

## 🔜 接下来要做（下次继续）

### 1. 重新测试新推荐公式
- fps 修正后需重新验证三个视频的推荐张数和耗时
- 验证矩阵：A(1min/30fps) + B(5min/60fps) + C(8min/30fps)
- 关注 B 视频推荐是否降到 ~93 且速度改善

### 2. 进一步优化推荐算法
- 根据测试数据微调公式系数
- 考虑是否需要为不同时长段分段调参
- 评估是否需要引入视频内容复杂度检测

### 3. 关键帧提取（研究方向）
- 调研 FFmpeg `-skip_frame nokey` 或 I-frame 提取方案
- 可能大幅减少解码耗时和提升帧质量

### 4. 提取体验优化
- 提取完成后自动跳转到最后一张截图
- 进度遮罩显示百分比

---

## 文件改动清单

| 文件 | 改了什么 | 版本 |
|------|---------|------|
| `src/components/Sidebar.jsx` | 更新关于界面的 GitHub 地址为作者哔站地址 | v2.1.2 |
| `package.json` | 2.1.1 → 2.1.2 | v2.1.2 |
| `electron-main.js` | 新增 `extract-frames`（全段扫描）+ `extract-frames-batch`（分段 seek） | v2.1.1 |
| `src/App.jsx` | `getRecommendedCount` 幂律+fps修正；`handleSmartExtract` 动态策略+dHash去重；`handleFileLoaded` 重置范围和duration | v2.1.1 |
| `CHANGELOG.md` | 追加 v2.1.2 | v2.1.2 |

