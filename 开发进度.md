# 开发进度

> 最后更新：2026-02-24

---

## ✅ 已完成（v2.1.0）

### 1. FFmpeg 引擎替换
- 提取引擎从浏览器逐帧 `video.seek` 替换为 FFmpeg 批量解码
- `electron-main.js` 新增 `extract-frames` IPC，一次 FFmpeg 调用提取所有候选帧到临时目录
- 临时文件在 `finally` 块中自动清理

### 2. 提取算法重写：分段优选
- **旧算法**：固定采样 96 帧 → dHash 去重 → 多样化补充 → 裁剪上限（输出张数不可控）
- **新算法**：视频等分 N 段（N=目标张数），每段过采样 4 帧，取最清晰帧输出
- 去掉了 dHash 去重、多样化补充等复杂逻辑
- **效果**：设置几张就输出几张，全片均匀覆盖，天然不重复

### 3. AI 跟踪兼容
- `runAiDetection` 支持 HTMLImageElement（兼容 `videoWidth` / `naturalWidth`）
- 批量提取时对每帧图片做 COCO-SSD 推理，确保竖图裁切准确

### 4. UI 改进
- 提取过程新增全屏进度遮罩（FFmpeg 解码中 → 分析帧 X/Y → 优选中 → 保存中）
- 图库清空改为下拉菜单：「仅清空图库」/「清空并删除文件」

### 5. 当前速度数据
| 视频时长 | 目标张数 | 耗时 |
|---------|---------|------|
| 48s | 20张 | ~3s |
| 8min | 20张 | ~13s |
| 8min | 100张 | ~18s |

---

## 🔜 接下来要做

### 1. 速度优化（优先级最高）
- **目标**：8 分钟视频 100 张降到 10s 以内
- **需要测试**：不同时长（30s / 2min / 5min / 10min）× 不同张数（10 / 30 / 50 / 100）的排列组合
- **优化方向**：
  - 分析耗时占比测试：FFmpeg 提取 vs 图片加载 vs Laplacian 计算 vs AI 推理，找出瓶颈
  - 图片加载：当前逐帧 `fs.readFileSync` → Blob → Image，考虑批量读取或直接用 Canvas
  - Laplacian 计算：当前对每帧计算，考虑降采样后计算（缩小到 320px 宽再算）
  - AI 推理：仅在开启竖图+跟踪时运行，非跟踪模式跳过
  - 候选总数上限（当前 600）是否可以进一步调优

### 2. 提取体验优化
- 提取完成后自动跳转到最后一张截图
- 进度遮罩上显示进度条（百分比）

### 3. 稳定性测试
- 各种编码格式（H.264 / H.265 / VP9）
- 极端时长（<5s / >30min）
- 极端张数（1张 / 500张）

---

## 文件改动清单（v2.1.0）

| 文件 | 改了什么 |
|------|---------|
| `electron-main.js` | 新增 `extract-frames` IPC |
| `src/App.jsx` | 新增 `loadImageFromPath`；`runAiDetection` 兼容 Image；`handleSmartExtract` 全面重写；`handleClear` 支持删文件；新增 `extractStatus` 状态 + 全屏遮罩 |
| `src/components/Sidebar.jsx` | 清空按钮改为下拉菜单 |
| `package.json` | 版本号 → 2.1.0 |
| `CHANGELOG.md` | 追加 v2.1.0 条目 |
| `ROADMAP.md` | 重写迭代计划，固定核心目标 |
