# 更新日志 (Changelog)

本项目所有显著的更新与修复将记录于此文件中。

## [2.0.6]

### ✨ 新特性 (Features)
- **TS / MKV 视频硬解支持**：引入 FFmpeg 极速数据流提取（`-c copy -map 0:v:0`），秒级解析原本浏览器无法直接播放的 TS/MKV 文件，避免无意义的全量重编码开销。
- **AI 智能人物跟踪 (Beta)**：基于 `@tensorflow-models/coco-ssd`。在“9:16 竖图”模式下开启，AI 将自动识别画面中的人物并智能调整裁剪偏移量（Crop Offset），始终确保主角处于画面核心位。
- **AI 惰性加载 (Lazy Load)**：视觉大模型由开机强加载改为“按需惰性挂载”，仅当启用“竖图”时才会异步在后台唤醒神经网络，彻底消除应用启动时 3 秒的界面无响应（拖拽禁区）假死现象。
- **零延迟原生光速拖拽**：于 `index.html` 注入底层事件拦截 `<script>`，接管 HTML5 默认拖拽校验，实现打开软件瞬间即可直接拖放视频文件的丝滑体验。
- **帧级逐帧快进 (Frame-by-Frame Seek)**：引入精确 FPS 探针（通过 `ffprobe`）。现在左右方向键的快进粒度已从原本的“秒”精确推断为“视频原生帧距（如 1/30s 或 1/60s)”。
- **快捷截图键新增**：除了鼠标右键，现增加系统级键盘 `S` 键用于快速截取当前画面。

### 🐛 问题修复 (Bug Fixes)
- **Electron 沙盒路径限权绕过**：修复由于 Electron 30+ 策略升级导致的 `file.path` 返回空值问题。引入 `webUtils.getPathForFile` 与探测 fallback 提取真实系统物理路径，解决无法向底层 FFmpeg 指明文件源的重大崩溃Bug。
- **拖拽全局禁入修补**：在 `App.jsx` 混入 `window` 级别 `dragover` / `drop` 的 `preventDefault` 全局监听，消灭鼠标移出视频框再移回时偶发卡出“禁止（🚫）拖入”光标的浏览器底层顽疾。
- **长按快进解码器宕机卡顿 (Throttle Stutter)**：引入了“按键蓄水池（Throttle + Debounce 防抖累加器）”。长按方向键时不再高频并发堵塞解码管线，而是把毫秒级的重复指令汇聚后，以最高 8 帧/秒的安全带宽提交给 `video.currentTime` 操作器，从而实现了德芙般平顺的在视频中的连续快进穿梭效果。

---
*注：请在每次版本迭代或核心逻辑修复后，主动更新此日志。*
