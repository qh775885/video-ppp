# 视频截图神器 - 下一代版本（v3.x）开发路线图

这份文档记录了经过讨论确立的未来核心开发方向，用于作为后续迭代的明确路标。下一代功能的核心目标是从“均匀盲截”彻底跨入“高质量动作精选”时代，专门为推特短片中的模特动作展示服务。

## 核心痛点与需求锚定
- **无须露脸**：不强依赖人脸判定，模特姿势和肢体动作才是焦点。
- **杜绝糊片**：因为高动态的走秀、扭动，原本卡在10秒固定时间点截取的瞬间往往充满严重的运动模糊。
- **防重复率**：10张截出来的图往往有3张是同样姿势的轻微位移，浪费提取名额。

## 核心功能规划（逐步拆解）

### 阶段一：防模糊机制（Laplacian 重心偏移）
- **实现原理**：当指定在每个时间区间（如第10秒附近）需要截取一张图时，算法不再死板地截第10.0秒。而是静默在 9.5秒 到 10.5秒 之间连续抓取 10 帧。
- **方差计算**：针对这10帧，利用 Canvas 结合边缘检测矩阵计算图像梯度的方差评分。
- **自动决策**：抛弃其中因为身体高速运动产生的带拖尾效应的废片，留下边缘最锐利、成像最清晰的那张作为该区间的代表帧保存。
- **前端表现**：面板上增加【防模糊优选】开关。

### 阶段二：动作去重系统（结构相似度 SSIM / 特征点对比）
- **实现原理**：在获取了一批高清的候选图片后，将其缩放为黑白像素矩阵。
- **差异值评估**：算法会依次对比两张照片的结构相似度和轮廓差异值。如果两张图片的姿态一致度高达 85% 以上，哪怕时间跨度较长，也会将其判定为“重复动作”。
- **自动决策**：淘汰重复度极高的后一张，强行从备用池或者下一时间轴拉取新图补充，确保十张照片覆盖这几十秒内十个完全不一样的肢体定格。
- **前端表现**：面板增加【避免姿态重复】开关及去重强度划块。

### 阶段三：面部补偿控制（可选开关）
- **实现原理**：在主攻姿态与去糊的前提下，新增一个可选增益逻辑。若模特带有面部且对视觉呈现十分重要，启用该选项后，引擎会在清晰去重的基础上，剔除那些刚好闭眼或者低头导致面目不可见的照片。
- **前端表现**：【保底包含清晰人脸】选项卡。

### 技术路线评估
所有上述算法（拉普拉斯锐度、SSIM特征比对）全部可以在现有的 Electron + Canvas 架构内利用**纯计算机视觉数学运算**跑通，无需引入重型 AI 模型，加载顺滑度可以达到 0 延迟，性能开销只局限在点击“提取”后的运算过程内。

---
*注：每次迭代将优先拆分一条主线功能，保证软件原有的纯净速度不受影响。*
