# 迭代计划

> **核心目标：易用 · 便捷 · 快速 · 智能优质**
>
> 一切迭代围绕这四个关键词展开，优先级从左到右递减。

---

## ✅ v2.0.9（已完成）

**Browser Video API 极限优化**

- 采样封顶 96 次 seek，48s 视频 ~6s 完成
- dHash 感知哈希去重，按结构而非像素判重复
- 时间分段保证覆盖率 + Laplacian 清晰度评分
- Canvas 复用零 re-seek 输出

**结论**：浏览器 seek 串行 ~100ms/帧，已到天花板，无进一步优化空间

---

## ✅ v2.1.0（已完成）

**FFmpeg 引擎替换——突破 seek 瓶颈**

### 目标
- 48s 视频提取从 ~6s → **~1-2s**
- 支持更长视频（5-10min）恒定速度完成

### 实现方案
1. **FFmpeg 批量解码**：一次命令提取所有候选帧到临时目录，替代逐帧 seek
2. **Node 子进程调用**：Electron 环境 `child_process.spawn` 直接调用
3. **分析管线不变**：提取后仍通过 Canvas 做 Laplacian + dHash 分析
4. **FFmpeg 内置打包**：捆绑 ffmpeg.exe，开箱即用，不依赖用户环境

### 注意事项
- 临时文件提取完成后必须清理
- 需处理各种编码格式的兼容性

---

## 🔧 v2.1.x（稳定打磨）

**长期可用版——即使不再迭代也能稳定使用**

### 目标
- 引擎稳定：大文件、特殊编码、极端时长等边界场景无异常
- UI 稳定：交互流畅、反馈清晰、无卡顿
- 速度拉满：在 FFmpeg 引擎基础上持续压榨性能

### 范围
- 修复 v2.1.0 引入的 bug
- 打磨边界情况和异常处理
- UI 交互微调，确保易用便捷
- 性能持续优化，直到无可优化

---

## 🔮 v2.2.0（远期）

**智能选帧——从算法去重到语义理解**

### 目标
- 速度仍为第一优先级，智能化不能牺牲速度
- 从"像素级去重"升级到"语义级精选"
- 自动识别最优画面（表情、姿态、动作节点）

### 可能方向
1. **姿态估计**：MoveNet/BlazePose 检测骨骼关键点，按关节角度差异判重复
2. **场景变化检测**：自动识别镜头切换/场景转换点
3. **运动能量曲线**：光流能量低谷（静止点）优先取帧
4. **美学评分**：NIMA 等图像质量评估，自动选最佳画面

### 定位（待定）
- AI 作为增强层叠加在算法选帧之上，不替代现有逻辑
- 商业模式待定（完全免费 / 赞助制 / Free+Pro）

---

## 优先级排序

| 优先级 | 方向 | 收益 | 复杂度 |
|--------|------|------|--------|
| P0 | FFmpeg 引擎（速度飞跃） | ⭐⭐⭐⭐⭐ | 中 |
| P1 | 稳定打磨（长期可用） | ⭐⭐⭐⭐ | 低-中 |
| P2 | 智能选帧（AI 增强） | ⭐⭐⭐⭐ | 高 |
| P3 | 美学评分 | ⭐⭐ | 高 |
